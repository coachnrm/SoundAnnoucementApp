window.scrollConsoleToBottom = function (element) {
        if (element) {
            element.scrollTop = element.scrollHeight;
        }
    };

function scrollConsoleToBottom(element) {
        element.scrollTop = element.scrollHeight;
    }